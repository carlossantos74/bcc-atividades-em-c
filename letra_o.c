#include <stdio.h>

int main()
{

  int a, b, c, d, p, s;

  scanf("%d", &a);
  scanf("%d", &b);
  scanf("%d", &c);
  scanf("%d", &d);

  p = a * c;
  s = b + d;

  printf("%d", p);
  printf("%d", c);

  return 0;
}